// function solution(picture: string[]): string[] {
//   const borders = "*".repeat(picture[0].length + 2);
//   return [borders, ...picture.map((row) => `*${row}*`), borders];
// }
// solution(["abc", "ded"]);
