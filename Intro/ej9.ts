// function solution(inputArray: string[]): string[] {
//   const longestWord: number = Math.max(...inputArray.map((el) => el.length));
//   const filteredStrings: string[] = inputArray.filter(
//     (str) => str.length === longestWord
//   );
//   return filteredStrings;
// }

// solution(["aba", "aa", "ad", "vcd", "aba"]);
